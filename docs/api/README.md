# API contract directory

The formal public OpenAPI contract is intentionally deferred to P5, after the P4 domain model is approved. P2 creates the documentation boundary only; no speculative product, offer, favorite, or promotion endpoint is defined here.
